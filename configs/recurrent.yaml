dataset_config: "configs/dataset.yaml"
artifacts_dir: "artifacts/recurrent"
random_seed: 42
model_type: "lstm"

model:
  hidden_dim: 32
  num_layers: 1
  dropout: 0.0

training:
  learning_rate: 0.0005
  weight_decay: 0.0001
  epochs: 8
  patience: 3
  batch_size: 128
  gradient_clip: 1.0
