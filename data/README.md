# Local Data

Generated market data is stored here and excluded from Git. Keep lightweight
metadata manifests in `data/metadata/` when they are useful for reproducibility,
but do not commit licensed, large, or personally identifying datasets.
